# 🎯 Current Task: Skill Polish & Visual Integration

## 🛠️ Objective
Now that the server-authoritative skill logic is implemented, we must integrate **Animations and VFX** to finalize the "Anime-quality" combat feel.

## 📋 Requirements
1.  **Animation Mapping:** Connect `SkillData.lua` references to `EffectController` logic.
2.  **Executor Expansion:** Create the first 3 starter skills within `SkillExecutors.lua`.
3.  **VFX Synchronization:** Ensure `SkillExecuted` triggers responsive client-side particles.
4.  **UI Feedback:** Update the "Spirit Qi" bar and combat text to reflect costs and execution success.